CREATE TABLE APP.PERSON("ID" BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY (start with 1, increment by 1), "NAME" VARCHAR(255));
CREATE TABLE APP.CLUB("ID" BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY (start with 1, increment by 1), "NAME" VARCHAR(255), "MEMBERS" VARCHAR(8192));